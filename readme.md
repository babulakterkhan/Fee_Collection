# ফি কালেকশন সিস্টেম

## 📝 প্রকল্প বিবরণ
এটি একটি ফি কালেকশন ম্যানেজমেন্ট সিস্টেম যা Firebase ব্যবহার করে তৈরি করা হয়েছে।

## ⚠️ গুরুত্বপূর্ণ নিরাপত্তা নির্দেশনা

### API কী GitHub-এ আপলোড করবেন না!
এই প্রজেক্টে ইচ্ছাকৃতভাবে API কী সরিয়ে রাখা হয়েছে। নিচের নির্দেশনা অনুসরণ করুন:

### 🔧 সেটআপ নির্দেশনা:
1. আপনার কম্পিউটারে প্রজেক্ট ক্লোন করুন
2. `config.js` ফাইল তৈরি করুন (নিচের উদাহরণ অনুসারে)
3. আপনার Firebase প্রজেক্ট থেকে কনফিগারেশন ডাটা নিন
4. `config.js`-এ আপনার Firebase ডাটা দিন
5. লোকাল সার্ভারে প্রজেক্ট চালান

### 📁 config.js উদাহরণ:
```javascript
const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_AUTH_DOMAIN",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_STORAGE_BUCKET",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};

firebase.initializeApp(firebaseConfig);
const database = firebase.database();